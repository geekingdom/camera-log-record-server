# 验证记录

本页仅记录已实际执行的验证和其边界，不能替代生产容量验收。

## 正式协议多路压测与目录尾部修复（2026-09-08）

新增 `benchmark_service.py`，使用独立合成串口资源，经真实 Telnet、正式 API、worker、MongoDB 目录及小时下载链路验证，按小时下载并流式剥离服务器时间前缀后比对源摘要。每路日志包含路由和连续序号。报告将完整性、目标发送速率、共同并发窗口及清理分别判定，另列完整 24 小时共同窗口是否满足。

首次 8 路、36 秒试跑失败，原因是目录节流更新依赖下一块日志：旧分卷和最后不足一秒的尾部未及时发布，直到 10 秒静默重连归档后目录才补齐。修复为独立定期发布脏目录水位；OPEN 与 READY 共用队列，数据库失败保留重试，迟到回调不回退归档状态。另用确定时钟测试复现批截止前收到小包后仍等待完整 100ms，现改为只等待剩余批窗口。

相同 8 路、每路每秒 1,200 行、每行 256 字节复测通过：345,600 行、88,473,600 源字节，含前缀存储 96,076,800 字节；共同发送窗口 35.988 秒，含任务创建、停止、压缩、下载和清理总耗时 48.324 秒。8 路均只建立一次连接，最终 8 个小时包共 16 个日志分卷，下载合计 2,976,924 字节，逐路摘要、序号及分卷顺序一致。检查 16 条 READY 目录对应的原始 `.log` 均已删除，合成资源软删除完成。

负载期间每秒节点采样中的写入 P99 最大值由修复前 208ms 降为 118ms；这是节点内部接收到文件写入的延迟指标，不等同于正式 API 文件可读的端到端 P99。合成正文具有确定性重复结构，压缩率不用于真实设备容量估算。本机 8 路短测不代表 500 路 24 小时或多节点基础设施隔离通过。

## 写入延迟测量与准入闭环（2026-09-08）

前端 17 项测试及生产构建通过。`browser_node_latency.mjs` 在 1440 和 390 像素视口验证延迟告警、首批待写、无样本、旧节点缺字段、长等待值及局部横向滚动。保持 tooltip 打开后直接缩窗，浮层自动关闭，三个告警数值仍唯一、可见且位于单元格内，无页面横向溢出。真实 API 浏览器全链路也通过，构建仍保留既有的大 chunk 提示。

后端 199 项测试通过，覆盖首块批次等待、实际写入完成、发布回调不混入写入延迟、失败写入不生成成功样本、未完成批次持续计时、高频样本不提前截断统计窗口，以及秒桶过期边界。节点与调度测试验证最慢一路 P99 上界或待写年龄超过 200 ms 时拒绝新会话，已有会话保持，恢复后允许接入；压力事件只在阈值跨越时写入。

500 路、每路每秒 1,200 行、256 字节/行、五秒合成回归：源数据 3,000,000 行、768,000,000 字节，发送耗时 5.001 秒，总耗时 6.649 秒；存储 834,000,000 字节，500 个归档共 11,276,368 字节，RSS 峰值 239,239,168 字节。逐路摘要和时间戳校验通过。这是短时正文回归，不提供 24 小时容量或 P99 达标证明。

受控升级采集进程后，四路原任务恢复 COLLECTING，串口 10003 保持 STOPPED。真实心跳出现 190 个完成样本、784 ms 的写入延迟值，节点暂停新任务准入；随后窗口值下降到 195 ms、344 个样本，准入自动恢复。`WRITE_PRESSURE_CHANGED` 已记录 NO_ADMISSION 和 NORMAL 两次转换，未为试验修改阈值或主动制造真实磁盘故障。

## 连接回收、审计与时钟回拨（2026-09-08）

本轮完整回归为后端 189 项测试、Ruff、前端 17 项测试和生产构建通过。新增测试覆盖 SSH 普通关闭错误的强制中止、Telnet 无响应关闭上限及取消清理、登录错误与清理错误同时出现时保留根因；审计覆盖搜索与下载取消、不存在令牌撤销、隔离依据递归脱敏、WebSocket 首帧认证拒绝与纯生命周期日志。

回拨测试模拟同小时一秒和跨小时回拨，解压后按块序号拼接验证无覆盖或重排；慢压缩与连续回拨确认每路最多两个后台待压缩任务并施加背压。未修改真实节点时钟。`CLOCK_ROLLBACK` 从写入器经采集器到运行事件数据库的贯通测试确认保留原任务、运行、会话和文件身份，且不覆盖任务状态。

500 路、每路每秒 1,200 行、每行 256 字节的五秒合成回归产生 3,000,000 行和 768,000,000 字节源数据；源发送耗时 5.035 秒，总耗时 6.728 秒，前缀后存储 834,000,000 字节，500 个归档共 11,272,413 字节，RSS 峰值 242,909,184 字节。逐路源摘要与时间戳前缀校验通过。该高度重复样本的压缩结果不代表真实设备压缩率，也不能证明 24 小时容量或 P99 延迟。

受控升级前暂停三路 SSH、停止串口 10002，确认旧 worker 的设备连接全部释放并退出。新 API/worker 启动后 33、34、35、10002 均恢复 COLLECTING，10003 保持停止。真实 WebSocket 不提交首帧时十秒后返回 4408，运行 JSONL 确认记录 TimeoutError 和关闭码；真实 API 浏览器全链路通过且无控制台错误。

## debug 当次失败隔离与最终回归（2026-09-08）

最新约定为失败仅影响当次 debug：无会话或运行级失败锁存，后续定时执行按配置间隔再次尝试，重连初始化也可重新执行；每次握手内部不重复提交口令。回归覆盖同会话首次失败后下一次定时握手成功、重连初始化重新握手、失败后普通命令及日志继续、Password 提示残留时不发送 ls。后端全量 171 项测试、Ruff、前端 17 项测试和生产构建通过。构建仍提示主包超过 500 kB。

旧采集进程释放全部设备连接并退出后，启动新进程，33、34、35 和串口 10002 均恢复 COLLECTING，10003 保持 STOPPED。33 曾在旧进程中因手动 debug 失败停止，本次用原有配置恢复，未额外向真实设备发送 debug 验证命令。新失败语义使用模拟设备验证，不能视为真实设备口令恢复验收。真实 API 浏览器全链路、ANSI/导航折叠及三种视口滚动检查通过，浏览器全链路无控制台错误。

## 滚动与默认 SSH 连接（2026-09-08）

默认 SSH 改为按任务账号密码认证，不要求 `known_hosts`，同一 IP、端口更换设备主机密钥不拦截。真实本地 AsyncSSH 服务验证首次无登记连接和同端口换密钥后的连接均成功；可选严格模式仍验证指纹。错误信息区分账号认证失败与严格模式主机密钥不匹配，两者均不自动重复认证。

采集进程升级前通过 API 暂停两路 SSH、停止串口 10002，确认旧进程无设备连接后退出。恢复后新增设备 10.41.203.33 达到 COLLECTING，本次运行已实际写入 739,659 字节；原 34、35 和 10002 同时正常采集。没有修改 33 的账户配置，也未登记或修改主机公钥文件。10003 继续保持停止。

`scripts/browser_overflow.mjs` 使用模拟任务的 14 条初始化命令、12 条定时命令和超长错误，在 1440x800、390x650、320x568 验证抽屉、诊断弹窗、节点/服务账号弹窗和小时表格。320 视口下抽屉正文实际滚动 3948px，最后一条命令可分别定位并聚焦编辑，添加命令和固定保存按钮可达；诊断正文滚动 1691px，日志表格横向滚动 380px且水平滚动条可见。各视口无页面横向溢出。末条命令与其后的操作不要求同时塞入短视口，分别滚动可达即可。

ANSI 显示处理采用 `strip-ansi`，另处理设备日志中的字面 caret CSI。前端 17 项测试通过，覆盖跨帧控制码拼行、未完成 CSI 跨顺序文件页、普通私有区正文保留和缓冲原文不变。`scripts/browser_ansi.mjs` 用模拟 WebSocket 驱动真实 LiveLogs，验证真实 ESC、caret 表示和跨帧颜色码不再显示，正文顺序正确，HTML 样例不产生 DOM 元素、不执行脚本；截图为 `output/playwright/ansi-live-1280x800.png`。所有设备正文仍留在原始采集及归档链路。

侧栏支持用户折叠展开并保存浏览器选择。浏览器在 1440x800 测得主体宽度 1224→1368→1224px、实时打印窗口宽度 1152→1296→1152px，四条模拟日志不变且 WebSocket 始终仅一条；截图为 `ansi-live-1440x800-expanded.png` 与 `ansi-live-1440x800-collapsed.png`。布局切换只调整容器尺寸，不重建采集或订阅。

## 控制台模块补齐（2026-09-08）

新增后台配置、独立日志工作台、服务账号和审计事件页面，重做控制台导航、任务状态和响应式布局。后端完整测试 164 项通过，Ruff 通过；前端 8 项单元测试通过，类型检查及生产构建通过，构建仍有主包大于 500 kB 提示。节点设置测试验证配置影响新连接准入和容量，保留期测试验证维护任务读取持久化配置，未修改真实保留期进行破坏性清理。

真实浏览器流程验证模板填充、任务草稿、实时打印更新、本地视图暂停/继续/清空、小时多选下载、独立工作台、片段正文读取、历史搜索完成及移动端布局，控制台异常为零。状态查看脚本在 1440、390、320 像素视口验证中文失败信息、PSH 状态、轮询更新及正文按文本渲染；测试不发送设备命令。

`scripts/browser_modules.mjs` 在隔离的模拟 API 下完成保留期保存、发现节点登记和编辑、服务账号创建与撤销、审计详情、日志工作台导航。精确断言五次预期写操作，1440 与 390 像素的四类页面截图检查通过，无页面横向溢出和控制台错误。截图和真实下载产物仅保存在被忽略的 `output/playwright/`，不进入仓库。

`scripts/browser_log_races.mjs` 延迟旧日期 A 的小时目录响应，先显示日期 B，再放行并等待 A 完整响应，最终仅 B 的表格数据保留。归档刷新期间清空旧选择，避免用新日期界面提交旧小时；作业创建与轮询也在提交状态前校验视图代次，检索忙碌时拒绝重复提交。

受控升级后确认旧采集进程退出且设备连接为零，恢复本机节点准入，再经调度恢复两路 SSH 和串口 10002。三路均达到 COLLECTING，两路 SSH 新会话确认 ASH。串口 10003 保持 STOPPED，不再次提交调试口令。平台默认保留 7 天，后台配置写操作使用模拟 API 验证，不为验收创建真实服务账号口令。

本轮五秒合成校验命令为 `python scripts/benchmark_collector.py --routes 500 --seconds 5 --output .local/collector-benchmark-modules`。共 3,000,000 行、输入 768,000,000 字节，源生成 5.092 秒、归档累计 6.812 秒，写入 834,000,000 字节，压缩 11,277,696 字节，主进程峰值 RSS 289,390,592 字节。500 个归档逐路摘要与时间前缀校验通过。此高重复样本不代表真实压缩率，不覆盖多节点协议端到端负载、24 小时持续运行或 P99 可读延迟。

## 同步失败收尾与 debug 疑似锁定

本轮完整后端 140 项测试、Ruff、前端 8 项测试和生产构建通过。为避免无必要的真实设备调试握手，未重启正在采集的进程；本轮代码在进程下一次受控重启后加载。

新增正文和索引 fsync 失败注入，旧实现在两种情况下均复现正文句柄未关闭。修正后释放文件句柄，保留原始日志和索引、不推进持久化水位、不发布归档；重复关闭或继续写入仍报告原同步失败。完整采集器故障用例确认设备连接释放且重复停止不会误报成功。注入使用测试临时文件，没有填满真实日志磁盘。

早期 debug 未返回完整密文的回归覆盖无响应、只返回普通提示符、只有 Password 提示和只有业务日志。当时采用任务失败停止行为，已被后续“失败仅影响当次命令”的需求替代，不再作为现行验收结果。错误仍区分“疑似锁定或响应超时”与锁定确证，不实际触发设备锁定测试。

同步收尾修正后的 500 路五秒合成复测为 3,000,000 行：源生成 5.031 秒、关闭归档累计 6.806 秒，写入 834,000,000 字节、压缩 11,283,237 字节，主进程峰值 RSS 293,486,592 字节；500 个归档摘要与时间前缀验证通过。输出位置 `.local/collector-benchmark-close-sync`。此结果仍不替代多节点 24 小时容量和 P99 延迟验收。

## PSH 自动切换与串口交错响应

新增 PSH 握手、解密提供器、旁路响应重组和运行失败清理回归后，后端 133 项测试通过，Ruff 通过；前端 8 项测试和生产构建通过。真实 telnetlib3 测试将响应按 13 字节分包并插入持续日志，验证完整原文密文传参、命令顺序、单连接及原日志保留。模式识别覆盖 ls 正常完成、不支持提示被分包、口令提交后再次用 ls 确认；握手失败不得自动重复提交口令。

两台 SSH 联调设备使用本地模拟口令完成 ASH 切换并继续初始化。串口 10003 能提取完整密文，但设备明确返回口令错误及剩余次数为零；随后仅执行 ls 确认为 PSH，任务保持停止，不再重试口令。该结果不能作为串口 ASH 切换成功证据。实际解密接口未调用，HTTP 合约仅通过模拟测试验证。原始设备日志仍保留设备输出的二维码和密文，服务事件不记录口令及密文。

加入模式观察快路径后的合成复测为 500 路、3,000,000 行：源生成 5.073 秒、关闭归档累计 6.861 秒，输入 768,000,000 字节、写入 834,000,000 字节、归档 11,290,470 字节，主进程峰值 RSS 250,068,992 字节。500 个归档逐路摘要及时间前缀校验通过。复现命令为 `python scripts/benchmark_collector.py --routes 500 --seconds 5 --output .local/collector-benchmark-psh-fastpath`；仍不构成 24 小时或 P99 生产容量证明。

## 真实设备会话

已使用两台真实设备完成两轮 SSH pause/resume 验证。每轮 pause 后等待 12 秒；记录结果为 `release=0`、`resume=1`。恢复后保持同一 `runId`，同时创建新的 `sessionId`，符合暂停释放会话、恢复重新建连的预期。

## Collector 摘要一致性

最新本地合成验证为 500 路、总计 3,000,000 行：

| 指标 | 实测值 |
| --- | ---: |
| `sourceElapsed` | 5.000 s |
| `total` | 6.636 s |
| 输入原始字节 | 768,000,000 |
| 写入字节 | 834,000,000 |
| 压缩归档字节 | 11,276,872 |
| 主进程峰值 RSS | 246,022,144 B |
| 时间前缀校验 | `true` |
| 摘要校验 | `true` |

数据源为合成的重复日志行。极高压缩率来自重复内容，不能代表真实设备日志的压缩率；该记录验证前缀与摘要一致性以及本地资源观测，不构成设备吞吐、端到端吞吐、24 小时稳定性或生产容量结论。

## 自动化测试基线

此结果包含每秒同步和单独压缩进程，RSS 不含压缩子进程。复现命令为 `python scripts/benchmark_collector.py --routes 500 --seconds 5 --output .local/new-benchmark`。

本次记录时后端测试为 83 项通过，前端测试为 8 项通过，Ruff 与前端类型检查及生产构建通过。构建仍提示主包超过 500 kB。浏览器验收覆盖模板填充、任务表单、实际时间戳日志、本地视图暂停继续与清空、移动端和原生下载，控制台异常为 0。此前 4 个真实 WebSocket 订阅合计接收 80 帧，协议错误、偏移错误及实时缺口均为 0。

新增回归覆盖：没有 `retainUntil` 的过期归档清理；保留近期、远端、被作业引用及下载保护的文件；候选扫描后新增下载保护的删除竞争；已发布归档恢复 OPEN 目录项；拒绝摘要错误和会话身份不匹配；不恢复 DELETING 文件；一路停止失败仍关闭其他设备连接。测试使用隔离临时目录和模拟数据库，没有对真实设备日志执行过期删除。

删除声明后的崩溃恢复另外覆盖文件尚存、文件已删除及远端 DELETING 记录：只续做本节点的删除，文件操作和目录删除均可重试。

请求日志新增六个测试场景：完整流结束才记录、响应头之前异常、响应头之后异常、取消传播、底层发送失败不累计未确认字节、401/422 响应保持状态与关联 ID。真机两个完整下载分别为 2,235,505 和 1,004,607 字节，运行日志的 `responseBytes` 与客户端接收大小一致；两次 Range 请求均为 206、100 字节且 `responseComplete=true`。浏览器流程再次通过，桌面和移动端截图已检查。

Telnet 保活增加真实 `telnetlib3` 与本地 TCP 服务测试，直接检查线上 `FF F1` 控制字节而非仅模拟 writer。验证保活后仍可收日志、发送命令，正文 `FF FF` 正确还原为单个 `FF`，主动关闭终止心跳并释放对端连接。对端持续发送 NOP 的情况下，采集器仍在默认十秒无正文时触发 IDLE_TIMEOUT；没有把保活保存为日志。本机库版本为 telnetlib3 2.6.1；Telnet 设备直连的硬件兼容性仍待验证。

新增实体串口服务器的 10002、10003 两个端口已按 TELNET_SERIAL 完成联调，账号密码留空，配置四条有序初始化命令。两路均收到实际打印，手动命令状态 SENT，停止操作成功，归档下载及 Range 206 通过；停止后端口连接数均为零，原有两台 SSH 各保持一条连接。注册脚本曾因只用 IP 构造幂等键发生第二端口冲突，现改为协议、IP、端口共同标识，并增加同服务器多端口回归测试。

尚未执行多台 Linux 节点上 500 路持续 24 小时的端到端验收，也未证明 P99 文件可读延迟不超过 200 ms。本机应用镜像构建在 Docker Hub 的 Python、Node、Nginx 基础镜像元数据请求阶段超时；完整容器链路改由 GitHub Actions 的 `container-smoke` 作业在隔离 Linux runner 上执行，具体通过状态以对应提交的作业结果为准，不能把本机直接启动结果当作应用容器验收。

## Docker 与副本集验证

提交 `2c812c41f7d6a93cce82f9c9b2fd13c298134e4e` 的 [GitHub Actions 验收](https://github.com/geekingdom/camera-log-record-server/actions/runs/34192052075) 已通过。`container-smoke` 在 Linux runner 实际完成镜像构建、服务启动、前端代理、初始化及手动命令、实时采集、停止释放、归档摘要和 Range 下载校验；作业耗时 2 分 8 秒，测试容器和卷已清理。这证明该提交的单机容器集成链路通过，不代表多节点生产容量达标。

容器验收配置生成新增两项测试：使用独立随机令牌及合法 Fernet 密钥、POSIX 文件权限为0600；已有配置文件拒绝覆盖。CI 不使用两台真实设备或开发环境凭据。

验收器自身的五项回归覆盖归档摘要损坏拒绝、ZIP 条目乱序按清单顺序重组、跨片段半行、实时偏移缺口、合法轮转与半行等待、时间前缀格式。`verify_container_service.py` 已通过本机前端代理试跑：两个初始化命令有序到达，十六行合成日志完成实时与归档比对，手动命令实际到达并返回 SENT，停止释放连接，下载及 Range 206 通过。该本机结果用于验证脚本，Docker 镜像链路仍由 CI 独立执行。

Docker 引擎可用。本轮用已有官方 MongoDB 7 镜像（摘要 `sha256:43fddee7e532a920f3dfdee9e8f4834398c155c26bcb92d790cc1cd3c630fc40`）在独立 `camera-log-acceptance` 项目完成三成员全新卷冷启动：初始化作业仅在一主两从健康后成功退出。重复运行初始化不重建拓扑；使用 `w=majority,j=true` 写入验证记录，三个成员重启后仍可读取。测试项目及其临时卷已清理，未使用现有开发数据库。

`node --test tests/deploy/test_mongo_init.mjs` 四项通过，覆盖首次选主等待、重复初始化、非94错误传播、成员不齐超时。Compose 解析确认默认 worker 使用稳定 ID、可通过服务名解析的 URL 和命名卷；生产 worker 配置要求显式提供节点 ID/URL。新空命名卷的目录权限复制已用独立卷验证，宿主 bind mount 仍需预先授权。

Docker scratch 实际导出源码上下文并扫描通过；曾发现前端子目录浏览器调试快照进入上下文，现已递归排除并删除本地验证导出，未发布该测试产物。`python scripts/verify_build_context.py` 使用纯合成文件验证三个必要源码路径与十九种敏感/产物排除场景，已接入 CI，无需外部基础镜像或真实凭据。

## 跨片段压测校验修正

压测脚本原先对每个归档独立按行剥离时间前缀，跨小时半行会误判。现在按块序号排列归档，流式跨文件重组逻辑行，再剥离一次前缀并计算源摘要，同时逐归档验证正文摘要。新增八个回归场景覆盖时间前缀和正文分段、归档回调乱序、重复、缺行、行乱序、跨路混写、缺失前缀、末尾截断和摘要损坏。后端完整测试更新为 91 项通过，Ruff 通过。

本轮复测命令为 `python scripts/benchmark_collector.py --routes 500 --seconds 5 --output .local/collector-benchmark-fragment-check`：输入 3,000,000 行、768,000,000 字节；源生成 5.001 秒、采集关闭与归档完成累计 6.718 秒；写入 834,000,000 字节、压缩 11,275,990 字节、500 个归档；主进程峰值 RSS 270,303,232 字节。逐路摘要及时间前缀校验均通过。输入仍为可高度压缩的合成数据，此次五秒复测不覆盖完整协议网络链路、P99 可读延迟或 24 小时稳定性。

## 节点磁盘准入竞态

新增八个节点回归场景，模拟任务分配后本机磁盘使用率升至 90%、95%、100%，验证均不建立新连接、低于 90% 后恢复启动；等待中的任务可显式停止并释放端点锁，编辑触发的受控重启意图保持有效；已有采集在 90% 时继续、95% 时主动停止并标记 ERROR。80%、90%、95% 和恢复正常均记录状态变化事件，同级别不重复记录。

完整后端测试为 99 项通过，前端 8 项测试、生产构建和 Ruff 通过。阈值测试使用模拟磁盘统计与模拟连接，不填满真实设备日志磁盘，不能作为真实 ENOSPC 故障注入的替代证据。

## 会话命令收尾隔离

2026-09-08 新增两个回归场景，修复前均复现任务级清理误取消命令。修复后，运行正常取消只将本会话的 QUEUED 标为 CANCELLED、SENDING 标为 UNKNOWN；其他运行、会话、任务和已完成命令保持原状态。采集器构造失败且未取得会话身份时，不执行命令清理。

本次完整后端 201 项测试、前端 17 项测试、Ruff 和生产构建通过；存在依赖弃用警告及既有前端大包提示。验证使用内存 MongoDB 替身与模拟采集器，未向真实设备发送调试口令，未重启正在采集的 worker。本项不等同于完整的分布式代次隔离或生产容量验收。

## 运行代次回写保护

2026-09-08 增加 18 个节点归属回归场景：分别改变运行、节点或代次，验证旧暂停、待连接暂停/停止及释放不修改后继任务、锁、运行、操作和本机实例；旧状态、压缩错误和调试回调不能覆盖新代次；模拟释放中数据库删除失败后，原归属保留，可重试完成收尾。原实现的 10 个旧归属场景、4 个回调场景及中途失败场景均先复现再修复。

完整后端 219 项测试及 Ruff 通过。测试采用 MongoDB 替身，验证数据库条件更新边界，不代表多节点网络隔离或自动故障恢复已验收。监督器异常处理、待连接任务多集合收尾及操作记录的跨集合原子性仍需继续完善。

CI 首次运行发现新增归属测试隐式依赖开发机 `.env` 密钥，18 项均在仓储初始化阶段失败。测试仓储现显式生成临时 Fernet 密钥并禁用环境文件；在 `ENCRYPTION_KEY=''` 条件下 18 项通过，避免以本机配置掩盖测试环境缺失。

## 任务操作按状态展示

2026-09-08 任务列表将生命周期按钮改为条件渲染，查看和编辑保留原入口。初始化、重连和调度等待可停止；SSH 采集中可暂停和停止；已暂停且期望仍为暂停时可继续和停止；已停止及无节点归属的失败任务可启动；暂停中、停止中和等待隔离隐藏重复或不可完成的操作。

前端 25 项测试和生产构建通过。`browser_task_actions.mjs` 使用 9 条隔离快照验证 1440 与 390 像素视口、按钮集合、页面无横向溢出及窄屏操作列可滚动访问，控制台错误和任务修改请求均为 0。桌面/移动截图已人工检查；`browser_smoke.mjs` 真实 API 全链路通过，控制台错误为 0。前端开发服务已加载修改，采集 worker 未重启。

## 监督器隔离与本地升级

2026-09-08 新增 19 项测试，覆盖归属消失、节点/运行/代次变化、任务阻塞、节点隔离后的主动关闭；旧释放错误不影响后继；当前关闭失败保留实例及端点锁；读取任务失败不误判为空快照；进程退出错误同样绑定原归属。运行时在重连复位与 CONNECTING 回调两处拒绝过期或阻塞领取，关闭/重连回调不覆盖 BLOCKED。回归先复现后修复，完整后端 238 项、前端 25 项、Ruff 和生产构建通过，保留依赖弃用和既有包体积提示。

受控升级前读取真实状态，仅暂停原 34、35 两路 SSH 并停止串口 10002。确认旧 worker PID 40014 已无设备套接字且退出后，启动新 worker PID 53644；恢复后两路 SSH 保留原运行 ID、代次从 25 到 26、均确认 ASH，串口按停止/启动产生新运行且代次从 11 到 12。新进程对 34:22、35:22、串口服务器 10002 各有一条连接。两秒观测三路文件分别增长 1,677、6,967、1,677 字节，时间戳前缀检查通过；浏览器实时与下载全链路通过、控制台错误 0。33、10003 维持 STOPPED，新建的重复 35 任务维持 BLOCKED；未改这些任务状态。

这些检查证明本次本机升级、关闭及日志恢复，不是多节点故障和生产容量验收。数据库准入与远端连接建立并非原子事务，接管仍须确认旧实例停止或外部隔离；多集合收尾持久恢复、命令预算 fencing、操作记录的归属绑定仍待完成。

## 手动命令领取与调试恢复入口

2026-09-08 新增 9 项回归。手动命令按数据库记录的任务与类型定位，核对当前运行归属及会话后领取；旧会话记录明确取消，发送正文和参数取自原子领取结果。记录已由收尾标记 UNKNOWN 时，迟到发送返回不再覆盖为 SENT。测试复现了旧归属、跨任务误分发及调用方正文副本被误用，修复后全部通过。

另修复 enqueue_manual 对命令通道阻断的判断：精确 debug 可进入已有安全恢复路径，普通命令仍拒绝。合成设备先报告错误口令和恢复未确认，后续用户 debug 经 Ctrl-C、ls 恢复并独立握手；两次握手各一次口令，唯一 reader 仍保存持续设备打印。未调用真实解密接口或向 10003 提交口令。

完整后端 247 项、前端 25 项测试、Ruff 和生产构建通过，独立审查定向 28 项通过。队列目前每个任务每秒消化一条，100 条旧会话记录可能拖慢当前交互或暂时占满配额；此吞吐边界与归属读取到发送间的非原子窗口仍待完善，不以本轮修复宣称完整命令 fencing。

## 设备资源、软删除与控制台改版

2026-09-08 完成资源优先的数据结构：海康网络设备通过 Digest 或 Basic ISAPI 认证后保存；串口服务器只填写名称和 IP。任务强制绑定资源，网络设备串口任务支持选择已有服务器或自定义地址，同端口允许多个任务。相同 IP、型号与短序列号使用相同日志父目录，下层按任务、运行及会话隔离。

资源软删除保留任务与日志，关闭所有直接或串口目标关联任务；只有终态、节点归属与运行锁全部清理后才记录 DONE。新增并发回归验证删除标记阻止迟到启动和编辑，调度领取也排除已删除资源。认证解析覆盖命名空间、错误状态、超时、响应上限及恶意 XML；设备凭据不通过查询回传。

设备资源页重做统计、筛选、资源身份和操作区，检查 1440、390、320 像素截图及弹窗滚动。资源浏览器回归覆盖认证过期响应、表单关闭重开、轮询保留草稿、资源编辑与软删除。确认弹窗回归验证 11 次修改：取消不请求，确认只提交一次，浏览器控制台错误为 0。

最终后端 279 项、前端 25 项测试、Ruff 和生产构建通过；保留依赖弃用及既有前端大包提示。重启本地 API 后运行 `verify_resource_workflow.py --browser`，真实本地 Telnet 模拟器的同端口双任务、浏览器实时日志与下载、采集中软删除、连接释放、删除 DONE 和归档连续序号全部通过。该流程不连接实体设备，不证明 500 路持续 24 小时容量或完整分布式隔离。

按开发阶段不保留历史结构的要求，本轮先停止旧采集并确认套接字释放，再运行显式开发重置，清理 9 个旧任务及其开发日志，保留模板、令牌、节点和平台配置。验收留下的合成任务已停止、资源已软删除，合成归档按软删除契约保留；未恢复旧实体设备任务。正常资源软删除与开发重置是不同操作。

## 重连命令积压与关闭压缩并发

2026-09-08 新增 8 项手动队列回归。修复前确认 100 条旧会话记录导致新会话入队返回 429，150 条旧记录导致节点优先分发旧命令；修复后当前会话单独计数与 FIFO 查询，每周期最多取消固定快照中的 100 条旧记录。覆盖后继会话在清理期间插入、失属、停止及记录已进入 SENDING 时不误取消；缺少有效会话身份时拒绝入队。清理记录任务 ID 与取消数量，不记录命令正文。

完整回归另发现时钟回拨后的 close 前台压缩绕过槽位，峰值可达到 3。现在所有压缩共用两槽限制，事件控制测试保持两个后台任务占槽，确认最终关闭等待槽位，再验证全部片段顺序及正文。

最终后端 287 项、前端 25 项、Ruff 和生产构建通过。确认无活动采集后更新本地 API 与 worker，实际 Telnet 模拟器经前端代理完成两个初始化命令、手动命令 SENT、16 行实时日志、停止释放、归档摘要及 Range 206；合成资源随后软删除，归档保留。未向实体设备建立连接或发送命令。并发入队的严格配额原子性、完整分布式 fencing 和 500 路 24 小时容量仍待验收。

## 定时预算与执行回调的会话隔离

2026-09-08 新增 13 项回归。修复前 10 项明确复现失属、停止、暂停、旧会话仍可占用预算，以及迟到成功覆盖 UNKNOWN 或后继会话执行。修复后预算入口核对任务、运行、会话、节点、代次及采集意图；Collector 的成功、失败、未知回调均传递原会话身份。内存待执行映射按会话隔离并在收尾释放，结果条件更新要求原执行仍为 SENDING；补充其他任务或运行回调不能占预算或取走待回写记录的验证。另以延迟状态回调复现首次定时提前触发，修复为 COLLECTING 状态成功发布后才启动定时器。

后端完整 300 项、前端 25 项、Ruff 和构建通过。确认本地无活动采集后更新 worker，扩展后的正式接口验收通过前端代理验证两次定时命令实际到达、执行次数序号 1 和 2 且停止前没有超预算发送；两个初始化命令、手动命令、16 行实时日志、停止释放、归档摘要与 Range 206 同时通过。验收器已纳入既有容器 CI；本轮未连接实体设备。此证据不能证明数据库归属读取到设备写入之间的原子隔离，跨集合事务、故障恢复和生产容量仍待完成。

## 资源模型下的实体设备联调

2026-09-08 在提交 `47ac8ef` 的服务上，通过正式资源 API 完成两台设备的 Digest ISAPI 认证及保存。34 返回型号 DS-2CD2T47SFWDV3-LTS、短序列号 GK4800565、版本 V5.8.31 build 260206；35 返回型号 iDS-2CD7T46G2/VX3-IS、短序列号 GA1548684、版本 V5.10.0 build 260612。串口服务器 10.41.203.8 同时登记为资源。凭据仅从受限本地配置读取，未写入提交。

分别新建一条资源归属的 SSH 任务，四条初始化命令为 outputClose、outputOpen、setDebug -m all -l 7 -d 111、prtHardInfo，保持顺序且每条延时 0.3 秒。两路均达到 COLLECTING 并持续落盘；采集 worker 对每台设备各有一条 SSH 套接字。此次没有发送 debug 或调用解密接口。

实际跨越上海时区 21:00 整点，20:00 至 21:00 的旧小时均成为 READY，21:00 起的新小时继续写入。两路正文抽样分别 189 和 1,418 行，全部匹配服务器时间戳前缀，样本未发现 Not Supported。随后通过正式 API 暂停两路，确认 PAUSED、节点归属释放且原运行 ID 保留；套接字为零，等待超过 12 秒仍无设备连接。恢复后原运行 ID 保持、新会话建立、两路重新 COLLECTING，各一条 SSH 连接。

多选下载每路的两个逻辑小时，各返回三个归档片段（含暂停前片段及恢复后当前小时固定水位快照）。34 的解压正文合计 1,397,861 字节、下载包 154,002 字节；35 合计 3,914,800 字节、下载包 300,980 字节。所有片段的清单摘要和字节数匹配，外层清单无缺失，Range 206 内容与完整下载前 100 字节一致。这是服务落盘与归档自校验，不等同于设备源端逐行完整性证明或真实日志整体压缩率报告。

实际浏览器实时与下载流程通过，控制台错误为零；检查真实资源列表 1440 与 390 像素截图。10002 和 10003 另登记为 STOPPED 的串口任务，未建立串口连接；10003 归属已确认的 34 设备并关联串口服务器，与其 SSH 任务使用相同 storageIdentity。本轮保留两路 SSH 持续采集，不恢复任何旧任务数据。尚未验证实体设备断电重启、串口锁定恢复、多节点隔离和 500 路 24 小时容量。

## 10 MiB 分卷与纯日志小时包

### 恢复后的分卷顺序

目录恢复时没有原会话起始时间，原目录与下载排序使用重置后的会话块序号，测试复现三个分卷按 1、3、2 排列。改为共用有序归并：同任务、同节点、同小时的持久分卷号优先保证流内顺序，再依据时间合并各节点队首；恢复记录从已校验旁路索引取首块接收时间。覆盖同小时多会话序号重置、下载重组、节点迁出后迁回及时间回拨。多节点模拟只验证排序算法，不证明实际迁移或基础设施隔离。

旧格式归档同样从内部索引恢复首块时间，旧会话的分卷号不作为全小时编号，而按任务、节点、运行、会话保持块序列。完整后端 320 项、Ruff 和独立审查通过。受控暂停两路 SSH 并确认套接字释放后更新服务，恢复原运行 ID；正式 API 命令、实时打印、纯日志包及 Range 验收通过。首次并行浏览器验收订阅了随后被接口验收停止的短时合成任务，审计记录确认该任务 ID；顺序重跑浏览器验收通过、控制台错误为零。此类修改同一环境任务的验收应顺序执行。

2026-09-08 将每任务每小时日志改为严格 10,485,760 字节分卷，使用六位递增 part 序号，小时包只包含日志。上节实体联调下载证据属于旧归档格式，不能用来证明本节新格式。

后续在当前分卷实现上进行 500 路短时合成验证：各路每秒 1,200 行运行 5 秒，共 3,000,000 行、768,000,000 字节源正文，含时间前缀保存 834,000,000 字节。源发送耗时 5.024 秒，含全部压缩收尾 7.209 秒，逐路摘要一致，生成 500 个小时包；主进程峰值 RSS 304,758,784 字节。该测试使用进程内模拟连接，不覆盖真实网络、MongoDB 和 WebSocket，也不证明持续 24 小时容量。

新增下载发布竞态回归：在复用资格校验返回后、下载文件链接建立前原子替换 canonical 小时包，旧实现错误返回替换包。修正为校验前固定每个本地源归档 inode，成员校验、单小时复用和多小时打包均消费固定版本；跨文件系统不能硬链接时，只快照所选成员及冻结水位，不复制整个共享包。完整后端 315 项通过后追加跨挂载点回归，定向 23 项下载、节点和进度测试通过。

8 路合成采集各以每秒 1,200 行、每行 256 字节持续 36 秒，共 345,600 行；源端 88,473,600 字节，加服务器时间前缀后写入 96,076,800 字节。源端发送耗时 36.002 秒、含收尾 36.255 秒，逐路序号及摘要一致。最终 8 个物理小时包包含 16 个日志分卷，每路分卷分别为 10,485,760 和 1,523,840 字节；压缩后共 1,189,107 字节，所有独立 `.log` 均删除。重复填充合成正文的压缩率不代表真实设备日志，8 路短测不代表 500 路 24 小时验收。

新增或适配回归覆盖分卷跨行、成员精确读取、同小时跨会话合并、压缩失败保留原始文件、发布事务恢复、原子替换后下载快照不变、旧大成员下载分卷、共享归档保留保护与旁路文件清理。后端 314 项、前端 25 项、Ruff 与前端构建通过。

本地服务更新前通过 API 暂停两路 SSH 并确认设备套接字为零，更新后保留原运行 ID 恢复采集。正式接口验收通过初始化、定时、手动命令、16 行实时打印、停止释放、纯日志小时包摘要及 Range 206；实体日志的浏览器实时/多小时下载验收通过，控制台错误为零，桌面及移动端截图已检查。额外资源流程验证同端口两任务独立采集、软删除关闭连接后仍能下载日志。磁盘检查确认验收任务 READY 归档中只有日志成员，其原始 `.log` 已删除。
