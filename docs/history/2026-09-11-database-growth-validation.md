# 数据库增长治理验证

核对起点：`8d2cacb`。本轮按实际源码、数据库和最新需求重新核对，没有复用摘要中的推测作为结论。

## 本机数据库

- 修改前只读观测：认证历史15条、审计22条、运行事件4条、请求记录50条。审计和运行事件仅有默认主键索引，请求记录已有旧名字的TTL及任务时间索引。
- 已在当前库实际执行 `Repository.initialize()`；认证/审计/运行/请求集合索引总数分别为8/6/5/10（包括主键及保留的旧索引）。
- 认证TTL回填先预览15条，再执行更新15条，复跑候选0条。未删除日志或core，未合并旧认证记录。
- API重启后健康200；经5173前端代理以已配置Bearer校验，资源认证历史游标请求与审计/运行/请求三个页面接口均200，内部并发代次不回传。

## 自动化

- 后端全量：1113 passed，107.42秒。随后内部字段投影、迁移上限、迟到观测及基准统计收尾，认证/保留/索引/仓库/API文档定向41项通过。
- 前端：94项通过，生产构建通过。
- 认证弹窗浏览器：1440和390视口通过，表格内部有横向滚动，外页面不溢出；使用模拟记录，不代表设备认证实测。
- Ruff及diff检查通过。此次不连接设备，不产生采集日志。

## 真实Mongo实验

`verify_event_index_migration.py` 在随机库预装旧索引后初始化两次：audit/request默认与筛选返回50条、扫描50键/50文档、IXSCAN且无SORT；runtime筛选扫描250条、命中索引但仍有派生时间SORT。结束后删除随机库。

`verify_authentication_history_concurrency.py` 首次运行发现随机数据库名达到64字符而失败，已改为短前缀。修正后真实副本集验证：16路相同完成时间的首次成功只形成一条count16；随后1路失败和16路成功同时提交，总成功次数32、失败1次、headRevision33。内部首次/最近代次证明成功聚合没有跨过失败边界。随机库已清理。

`benchmark_db_growth.py --rows 20000 --compare-indexes` 为每个集合生成20000条元数据。普通认证首屏扫描20键/20文档，审计和请求筛选扫描50键/50文档；旧skip5000认证分页仍扫描5020键，因此第三方推荐游标。runtime需要扫描2500个候选并派生排序，不能用IXSCAN宣称整条查询无额外成本。此为合成样本，不能证明生产长期增长或500路采集容量。

最终基准已修正聚合统计：`cursorReturned=2500`、最终`nReturned=50`，不会把中间候选数误当最终返回数；对应回归通过。样本时间统一为当前UTC前一天，避免请求记录在实验中意外被30天TTL清除。修正后20000条基准重跑通过，耗时约2.30秒。

## 保留边界

90天指固定天数，按日聚合的次数是整段计数；不提供任意子区间的精确逐次认证统计。审计和运行事件当前仅有保留预览计划，没有删除执行器；命令和操作仍保留，未以终态作为安全删除证明。旧数据TTL回填单次默认5000条，需单维护进程在低峰重复执行，没有全局迁移锁。

## 在2cfaab0基础上的恢复复核

最新可见请求仍是索引与认证历史治理，交接摘要中的SSH实测计划未作为本轮任务执行。重新读取源码及当前业务库，确认认证/审计/运行/请求索引数量为8/6/5/10，认证保留配置90天，无缺少expiresAt的记录；未再次执行历史回填或操作设备。

重新运行认证保留、认证查询、事件索引、记录保留和基准统计五个测试文件：28 passed。真实Mongo索引迁移脚本再次通过，审计与请求默认及筛选查询均返回50条、扫描50键/50文档，无COLLSCAN或SORT；运行事件节点筛选扫描250条并存在派生SORT。真实副本集并发脚本再次确认32次成功计数、1次离线记录及headRevision=33，成功聚合不跨越失败边界。两个脚本使用独立随机库并在finally清理，没有产生设备采集日志。以上为本轮实测，不以历史全量或生产容量证据替代。
