# 四项功能改进验证记录

本轮基线为4901021；下面是本轮实际证据，不代表500路全天容量或物理多节点隔离已验收。

功能提交`4953970`已推送；[CI34917184543](https://github.com/geekingdom/camera-log-record-server/actions/runs/34917184543)核实为completed/success，后端、前端、完整容器、独立组件、原生部署、NFS及中文提交共七项全部成功。完整容器内新增的速率配置竞争和增长治理真实Mongo步骤均成功。后续文档整理不改变该功能提交的源码。

## 已执行

- `verify_input_admission.py`：真实Mongo副本集在领取快照期间收紧配置，领取回滚且无孤立run/lock；放宽后恢复。
- `verify_growth_records.py`：真实Mongo摘要写入后注入失败，命令及审计删除回滚；1001命令按100条分段，最终计数1001；活租约拒绝并发维护，过期可恢复。`runs`、`commands`、`idempotency`、`operations`、`jobs`五类查询各返回10项、扫描10键/10文档，无阻塞SORT。随机库均在finally删除。
- `frontend/scripts/verify-workspace.mjs`：20项生产构建页面验证通过，覆盖1440/390/320布局、设置保存、跨文件补读、权限与任务切换。前端最终156项通过。数据为模拟接口，不能替代实体设备。
- `test_log_gap_catalog.py`共3项通过，新增回归确认中间`DELETED`文件返回`FILE_UNAVAILABLE`，后续页保留相同问题。前端跨页合并相同目录提示，日志正文不去重；收尾前端156项、构建及缺口专项浏览器再次通过，控制台无异常，390截图已检查。
- 受控重载前核对无`RUNNING`/`PAUSED`任务、活动作业、任务归属和端点锁；旧API PID 39383与Worker PID 45865正常退出，随后启动API PID 17073与Worker PID 17074。`8000`、`8001`、`5173`健康检查均为200，`local-dev`新心跳携带`inputBytesPerSecond=0`，增长记录维护租约行已出现。归档/清理/缺口/设置相关定向24项通过。该状态只证明本机本轮已加载工作区版本，不证明目标部署或物理跨节点环境。
- 隔离真实API/Worker/WebSocket/小时下载验证通过，控制台无意外错误，随机库与临时日志目录已自动删除。最初误以为脚本支持`--help`，未设置Playwright路径而失败；子进程均退出、随机库清理后，使用显式Playwright路径运行成功。
- 本机16路每路1200行/秒、3秒，共57600行，源14745600字节，含时间前缀保存16012800字节，压缩203540字节；逐路摘要与时间前缀一致，总耗时3.154秒。此为高度重复的合成数据，压缩率不可用于真实容量估算。
- 上述合成采集使用本轮新建的`output/four-gaps-collector.json/`目录（参数为目录而非报告文件），仅含16路自建模拟日志。进程已正常退出，无数据库任务或后台作业引用；记录指标后定点删除，保留本报告。

## 验证边界

第一次后端全量结果为1415通过、11失败；失败均来自新增API字段尚未补入站内文档描述。补齐字段与接口参数说明后，文档11项通过；后续全量中两项导出用例仍按立即重试判断，已按有界游标回绕合同修正并验证，最终后端全量为1433通过。其后新增已删除片段回归和维护启动检查修正，相关24项通过；导出相关定向53项通过。Ruff与diff检查通过。上述结果覆盖本机实现，提交CI需按实际提交单独核对。

没有访问34/35，没有新增真实设备日志，没有清空全局日志目录或修改全局日志保留期。
