# 认证历史游标页面验证

代码起点为7d5e383。认证90天保留、按日连续成功聚合和数据库索引已存在，本轮完成前端接入游标，未重复修改保留或聚合逻辑。

## 行为与边界

首屏显式发送cursor空字符串，避免通用query省略空值后进入旧count/skip分支。每页20条，提供前后翻页及刷新，不统计总数、不提供任意跳页。分页器只保存当前正文和已访问起始游标，上一页重新读取。失败不推进页码，允许重试；筛选、资源切换和关闭使旧成功及失败响应失效。认证累加、新增和TTL删除期间不保证跨页数据库快照。

## 验证证据

- 旧前端浏览器验收在首屏没有cursor时失败，新实现通过。
- 后端全量1234项通过；认证专项14项通过，首屏与后续页测试禁止count_documents调用。
- 前端115项、生产构建通过；分页器和API专项9项覆盖空游标、失败重试、旧响应隔离及当前页正文缓存边界。
- Ruff和git diff检查通过，独立审核无阻塞问题，缓存说明注释已修正。
- 认证浏览器脚本通过首屏、前后翻页、失败重试、刷新及筛选；1440与390截图已检查，页面无横向溢出，表格内部可滚动。接口使用模拟响应，这部分不证明真实Mongo性能。
- 额外执行verify_isolated_browser_smoke.py，真实隔离API/Worker/WebSocket和小时下载通过，控制台异常0；临时数据库、日志目录和子进程均已收尾。不访问真实设备。

本轮没有新增真实设备采集日志。两万条数据库Explain历史报告仍显示旧深页5020键和运行事件派生SORT，不能用前端分页修复宣称所有集合查询已优化或500路全天达标。
