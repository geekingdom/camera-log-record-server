# 手动命令提交审计

## 变更范围

恢复时核对 HEAD `e8d2887` 和总表 R28：Docker 与原生部署共享普通 HTTP 兼容的 UUID 生成实现。重新查询 `443c336` 的 CI 34338617932，结论为 success。本轮不重新实现该修复，不重启采集节点。

手动命令此前准入事务结束后才写审计，审计失败可能留下可发送命令。新增 `commands/manual_submission.py` 将命令、成功幂等映射、队列准入和审计一起提交。相同请求在任务停止后仍可重放原命令；路由先校验当前权限。设备发送不属于数据库事务，历史 PENDING 映射只返回已有命令，不补发或补造审计。

## 验证

本轮后端全量 `730 passed`，`ruff check backend tests scripts` 及 `git diff --check` 通过。前端源码未修改，沿用已确认的 HTTP/IP CI 证据；新增事务 CI 需在推送后单独核验。

`scripts/verify_manual_submission.py` 使用随机独立 MongoDB 数据库及临时日志目录，经正式 ASGI 接口注入审计异常、取消、并发请求和提交后确认丢失。确认失败时命令、幂等映射、审计及任务声明版本共同回滚；并发和停止后重试仅返回原命令。全部检查通过，最后验证临时数据库和目录已删除。脚本已加入 Docker 集成 CI。

审计取消场景的预期 HTTP 500 来自故障注入；数据库回滚已独立检查，不是实体设备失败。本轮不发送设备命令、不删除实体采集日志。新增代码未加载到本机运行的 API，目标服务器产品验收与集群全天容量验收仍未完成。
