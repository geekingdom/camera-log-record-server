# 2026-09-09 账户、来源 IP 与部署验证

本轮父提交为 `fc78034a1e15286c65be51e7513cbfc34f000b48`，状态入口为 [当前状态总表](../implementation-status.md)。

## 最终合同

- IP 白名单只读取平台访问客户端来源，覆盖登录前 API、登录后权限、实时订阅及下载；不读取资源 IP，不进入 SSH/Telnet 连接与调度。
- 多个命中规则权限合并后与账号权限取交集；IPv4、IPv6 单地址及 CIDR 均支持。保存有版本检查和当前管理员防自锁检查。
- 开发与正式管理员初始值按用户要求为 `admin` / `asdf!234`，首次改密。已有数据库账号不被环境变量或重复部署覆盖。
- 新增/编辑/停用子账户与权限；普通浏览器使用可撤销 Cookie，第三方保留 Bearer Token。
- Linux `./deploy.sh` 准备配置、构建与启动服务，验证健康与 worker 心跳；再次执行保留配置与卷。

## 本轮证据

- 后端全量：564 passed，33.56 秒；727 条现有测试依赖弃用警告。
- Ruff 通过；前端 25 项测试及 TypeScript/Vite 构建通过。
- 构建上下文：3 个必要文件与 19 个排除场景通过；副本集初始化 4 项检查通过。
- `codegraph impact enforce_ip` 的调用链在平台 API/身份入口内，不包含采集连接或调度模块；以源码和接口测试作为最终依据。
- 开发产物清理预览：0 个可清理压测下载副本，0 字节。本轮测试使用临时目录/模拟接口，不创建真实采集任务。
- 开发 API 在前一阶段已更新并核验初始登录，本轮未修改真实管理员密码或启用真实白名单，未重启真实采集 worker。
- 补充下载与来源边界后，下载/来源集成 6 项回归通过；用户/权限/来源/下载组合 14 项通过。覆盖 Cookie 下载成功、撤销下载权限后 403、停用账户后 401，以及允许来源创建并启动白名单外串口目标任务。
- `scripts/browser_user_auth.mjs` 模拟 API 浏览器验收通过，桌面账号页及移动端来源 IP 页截图人工复核。标签原先看似空白是截图捕获了 Element Plus 淡入过渡，脚本现等待实际 opacity 与尺寸到位再截图。
- Linux 验收脚本增加真实来源白名单启用/关闭链路；最终复核修复两次 PATCH 遗漏 CSRF 请求头的问题。新增 `test_auth_smoke.py` 通过正式 FastAPI 路由和隔离数据库运行整个脚本；与部署测试合计 16 项通过。

## Linux CI 结果

功能提交 `2c62fdbe80b1d55d518e333844fa31a83c8f1939` 的 [CI 34304331562](https://github.com/geekingdom/camera-log-record-server/actions/runs/34304331562) 全部成功：后端 567 passed（832 条依赖弃用警告），前端、中文提交校验、container-smoke 均 success。

真实 Linux 容器步骤通过：一键部署两次且保留环境摘要与卷；代理采集命令与归档下载；管理员首登改密、精确来源白名单启用/关闭、伪造 XFF 不改变来源、只读子账户和停用会话；隔离/调度/定时预算事务与日志目录水位；8 路 60 秒协议分卷归档验收。结束后隔离容器和测试卷清理成功。

CI 已预装 Docker，因此不证明裸机 APT 安装路径；该路径仅完成脚本与编排测试。短时容器协议检查不替代 500 路全天容量验收。模拟浏览器截图和真实代理 API 验证分别记录，不将两者拼称为真实浏览器全产品验收。
