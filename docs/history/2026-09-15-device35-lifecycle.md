# 35设备隔离生命周期验收

源码版本：`282f982`，后续`5ecd00c`只更新验证文档。2026-09-15本机确认34、35的22端口可达后，仅对用户授权的`10.41.203.35:22`执行正式API与Worker链路，不对34进行SSH登录。

## 环境与操作

使用本机Mongo副本集的随机`device35_lifecycle_`数据库、随机回环API/Worker端口和临时日志根。现有平台没有活动采集任务，现有API与Worker进程保持运行。设备凭据从本机受限清单读取，没有写入报告或Git。

通过正式资源API完成摘要认证，创建关闭Coredump和CPU/内存监控的海康资源；创建一条主机SSH任务，初始化命令为四条独立有序配置：`outputClose`、`outputOpen`、`setDebug -m all -l 7 -d 111`、`prtHardInfo`，每条延迟0.3秒，无定时命令。PSH模式设为disabled，本轮没有debug、重启、core触发或额外空闲注入命令。

执行既有`scripts/verify_ssh_lifecycle.py --cycles 2`，显式指定本轮任务ID和隔离Worker PID。临时编排器只负责隔离服务启动、资源任务创建及最后清理，生命周期断言复用仓库脚本。

## 结果

- 任务ID：`f22cd40dea6c4e8db11f7d0e418bf6ee`。
- 两轮暂停完成后TCP FD和Mongo SSH名额均为0，等待12秒后再次核验仍为0。
- 两轮恢复均保持原runId并创建新sessionId，每次正常采集时本机对应端点TCP FD和名额为1。
- 最终正式stop成功，任务STOPPED、节点归属释放、全部运行代次名额为0。
- 文件目录包含3个会话，归档共有3个日志成员，合计2,022,642字节；归档内没有非日志成员，每个成员正文以服务器时间前缀开始。
- 子进程退出后，独立Mongo查询没有遗留`device35_lifecycle_`数据库；`lsof -nP -iTCP@10.41.203.35:22`没有连接记录。临时目录上下文退出完成，测试日志和归档没有保留。

## 证据边界

补充实测（同日）：使用相同隔离方式，对任务`093e6b34d7ad41dd970ccab2412aa48c`执行一次暂停恢复和`--verify-idle-reconnect`。发送`outputClose`后，旧会话`a7c80f5f-c2eb-4a7f-ad75-14818c55173a`记录`IDLE_TIMEOUT`；自动重连保持同一run、创建新session，端点连接和名额均为1，包含43次安静窗口FD采样。最终停止成功，3会话归档共3个日志成员、1,985,616字节，随机库及临时目录已删除。本轮没有重启、debug或NFS操作，也没有源端独立摘要，不作为零缺失证明。

独立复审确认隔离服务和数据库范围。四条初始化命令会改变35的打印开关/级别，这是本次授权联调行为；脚本无法统计外部平台或人工终端的SSH连接，平台名额与本机FD只代表本次可观测范围。

这是35主机真实设备的两轮正常暂停恢复验收，不是设备重启、长时离线或物理跨Worker接管。12秒窗口为暂停完成及等待结束的采样断言，不能据此排除采样间任意短暂连接。初始化配置及流程完成不代表逐条业务命令结果已被解析确认。归档检查证明收到正文及格式，未取得源端独立摘要，不能宣称实机全程逐字节零缺失；逐路完整性仍依赖合成源校验和目标容量验收。
