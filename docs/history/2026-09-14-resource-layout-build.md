# 资源筛选布局与构建验收

日期：2026-09-14。代码基线：4fadbd3。当前范围为型号/序列号筛选、工具栏重叠、趋势图分块和此前独立部署诊断收尾。

## 实现

- 资源API已有model/subSerialNumber字面子串查询，使用re.escape且忽略大小写。本次补前端独立输入、显式请求类型、组合参数与后端契约测试，无新增数据库索引或正文存储流程。
- 工具栏原单行flex在增加条件后挤压。改为自适应网格与独立换行的选项/操作区，按钮不收缩；复用已有表格横向滚动。
- 趋势组件此前已经按需加载且使用ECharts按需导入。本次按echarts/zrender稳定库边界分块，未提高警告阈值。563.36KB大块拆为弹窗13.78KB（gzip5.93KB）、echarts374.08KB（127.73KB）、zrender176.99KB（59.25KB）。主入口不直接导入图表块；不宣称总下载量减少或加载耗时改善。
- 独立组件部署非零退出现在提供固定类别及排查提示，第三方原文不回显。审查发现空格密码、Basic及自由文本凭据无法仅凭字段规则可靠隐藏，已先用失败测试复现，再将输出改成固定类别，不输出匹配行。不扩展为所有超时路径的诊断。

## 验证

- `.venv/bin/pytest -q`：1394项通过，162.85秒。随后补两项诊断边界测试并限制输出，诊断与独立组件相关37项通过；不把新增测试数量推算成一次新的全量结果。
- `npm test`：31文件155项通过；`npm run build`通过，无500KB警告；Ruff和diff检查通过。
- `frontend/scripts/verify-workspace.mjs`：20项生产预览浏览器脚本全部通过。资源筛选1920/1440/1024/390/320及三种桌面宽度侧栏折叠状态通过控件矩形、内容尺寸、页面溢出断言；1440/1024/320截图人工检查正常。趋势图12次请求、canvas与切换通过，无控制台异常。
- `browser_resource_identity_filters.mjs`：14次请求覆盖型号/序列号/名称、类型、创建者与软删除组合；清空单条件保留其它条件，第2页多选后筛选回第1页且清空选择。
- 隔离真实API/Worker/WebSocket/小时下载通过，随机数据库和模拟日志目录已删除。首次误以为脚本支持`--help`而执行，因未提供Playwright模块路径失败；显式提供已有模块路径后通过。失败与成功两轮均由finally关闭进程、删除随机库和临时目录，无真实设备实验数据。
- 基线CI34840657957首次component-smoke只记录“database独立部署失败、无容器”，原始输出丢失，无法判断根因。未改代码重跑该作业后七项成功；不能将猜测的网络/镜像限流当作已确认原因。本机Docker未运行，本次诊断变更的Linux集成由新提交CI核对。

截图保留在`output/playwright/resource-identity-filters-*.png`及`resource-metrics-*.png`，不提交生成产物。前端开发服务5173已确认HTTP200；无需重启API/Worker来加载这次纯前端和验证脚本改动。生产500路全天、跨机故障及目标环境部署边界沿用当前状态总表。
