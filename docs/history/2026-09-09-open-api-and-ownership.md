# 开放接口、所有权与实时双协议交付记录

## 变更范围

- 站内API文档从实际OpenAPI构建目录，展示请求、响应、schema、权限、错误和接入指南；导航紧随后台配置。支持HTTP的ws和HTTPS的wss，保留端口及IPv6主机。
- 工作台使用分页任务选择窗口，支持资源筛选、状态轮询、创建人和北京时间；异步迟到结果不能替换新选择。
- 资源IP规范化后原子占用，与资源、幂等记录及审计共同事务提交；软删除释放占用并保留日志。
- 有效用户共享读取资源、任务、日志；写入要求创建者或管理员，同时保留功能权限和来源IP约束。服务令牌绑定用户、实时继承权限和生命周期，可配置永久有效；已过期令牌不能通过编辑复活。
- 模板按创建者隔离名称，可个人使用、多选共享或管理员全局共享；任务保存独立命令快照，模板撤销/删除不影响已保存任务和成功幂等重放。

## 验证证据

- 后端全量765项、前端65项、生产构建及Ruff通过。随后API指南和最小共享用户示例定向10项通过。
- 文档浏览器1440/390/320通过，无横向溢出；HTTP当前地址和ws/wss并列示例正确。账号与共享模板1440/390通过。
- 真实Mongo地址占用验证：12并发仅1成功，审计失败回滚，软删除释放。服务令牌验证覆盖创建/撤销/编辑回滚和提交未知只读确认。随机数据库与临时目录均已删除。
- 独立审查修复过期令牌可延长和共享用户目录权限描述两项缺陷；前端验收发现并修复永久开关未提交null的问题。

## 本机部署边界

隔离全链路：`scripts/verify_isolated_browser_smoke.py` 已以随机Mongo库、独立API/Worker/Vite、本地Telnet源通过正式Cookie普通用户建资源和任务、真实WebSocket、模板与编辑抽屉、工作台、小时下载、内容读取、异步检索及桌面/移动布局；零意外控制台错误。临时库、进程、日志和下载副本已回收，未使用真实设备。该次验收发生在“随时查看服务账号口令”新增需求前，不代替新口令功能验收。

2026-09-09本轮将API从52645更新为65766，健康检查和共享用户目录200，API目录67项。1960个任务更新前后均STOPPED，包含任务身份、状态、运行、会话、代次及节点字段的摘要相同：`c268401c7fd0db013284c083180a1f3c153aec13555de463993dbd1346a1ef10`。Worker25871未重启。PID只代表该次验证时点，恢复必须重新核实。

本轮未向真实设备发命令或采集新日志。HTTP代理和TLS示例均包含Upgrade；原生部署复用Docker同一Nginx站点模板。wss的ASGI握手测试不等于目标服务器真实证书部署验收。500路全天及物理节点隔离继续保留为未完成项。

## 新增口令查看

口令由管理员或绑定用户本人随时查看；服务端加密保存，公开列表移除密文和散列，查看审计无正文，成功响应no-store。审计后再次核验版本和归属，防止并发改绑返回旧口令。旧摘要无法还原，由管理员明确确认重新生成；轮换只改密钥和版本，不复活过期/撤销状态。真实Mongo验证轮换审计回滚、确认丢失只读恢复及查看失败不返回明文通过。

后端最新全量777项通过。本机API随后从65766更新至72913，目录69项，任务摘要与Worker保持不变。前端增加“我的服务账号”，普通用户仅查看和复制自己的口令。

## 模板页切换异常

用户报告模板页后导航标题变化但正文残留。只读字段统计发现当时17条模板均缺少sharedWith，前端直接读取length触发渲染异常。以缺共享字段夹具复现后，前端改为安全读取，后端public_template统一补齐sharedWith=[]和sharedWithAll=false。桌面3轮每次重新进入模板后切换资源/日志/API/配置，以及390/320视口均通过，无残留表格和控制台异常。

模板修复后本机API最终更新至73964，正式模板列表200且共享字段完整；1960任务状态摘要一致，Worker25871未变。后端模板/API目录/审计定向17项通过。

最终前端67项、类型检查、构建通过。生产预览四套mock浏览器通过：工作台1440/390/320、API文档69项与导航顺序、管理员/本人服务账号、历史模板导航切换。HTTP无Clipboard API时可回退复制；查看口令对话框使用通用文案，组件卸载会丢弃迟到响应并清除内存口令。只有旧摘要专用错误码允许提示管理员重新生成。

## 阶段最终收尾：批量、创建人和时间

R40补齐资源/模板批量软删除、任务批量启动/停止/暂停/继续；逐项检查所有者和状态、二次确认、串行提交，部分失败继续且不自动重试。筛选或卸载后停止后续提交；已开始的请求按实际响应记录。R41补齐三页创建用户筛选、普通用户默认本人/管理员默认全部、查看全部开关、创建时间及删除时间；任务列明确为资源删除时间。最小创建者目录保留历史用户，模板全部视图仍受共享权限限制。模板软删除不影响任务快照，同创建者的已删除名称仍占用。

最终前端修正资源批量处理中筛选变化后的忙碌状态回收、模板旧筛选响应丢弃，三页时间固定Asia/Shanghai。随后重新生产构建、类型检查及70项测试通过。六套生产浏览器全部通过：工作台、API目录（70项）、账号/模板共享、缺共享字段历史模板导航、资源批量删除、任务批量控制；覆盖1440/390/320、默认范围/创建人筛选、取消零请求及部分失败后继续。桌面资源批量和320像素任务截图人工检查，宽表提供横向滚动。截图位于忽略目录output/playwright，不包含真实账号口令。

最终后端全量783项通过（62.25秒），Ruff通过。测试依赖仍有Starlette/anyio/mongomock弃用警告，无失败。真实Mongo模板软删事务验证包含唯一键冲突回滚并删除临时库；隔离全链路和地址/令牌验证的范围见上文。

最终本机API更新至87886，正式创建者目录/模板列表/API目录均200。OpenAPI有69个HTTP操作，站内另列1个WebSocket，共70项。更新前后1960任务的desiredState/status均STOPPED，任务摘要仍为c268401c7fd0db013284c083180a1f3c153aec13555de463993dbd1346a1ef10，Worker25871不变。首次维护预检误用不存在的actualState字段而中止，未重启服务；核验源码实际字段status后再执行，未据摘要猜测状态。

用户要求的提交前全项目梳理已反映到总表：数据库命令预算原子性已完成，设备socket不确定性、物理隔离、持久作业恢复、500路全天/独立文件P99、目标TLS/多机/宿主机重启仍未验收。提交本阶段后停止需求推进，等待手动继续；新提交CI结果另行核验，不以基线CI替代。
