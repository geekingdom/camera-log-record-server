# 数据库请求恢复复核

2026-09-11，起点提交 `afb9925`。最新用户要求是按当前查询建立有用索引，认证历史改为三个月保留，并允许连续成功计数聚合。恢复摘要却指向前端导航，因此先对照源码及真实数据库；已有 `376477c`、`35b269c` 的数据库实现无需重复开发。

## 当前实现与实际库

- `resources/authentication_records.py` 使用每资源head记录及事务串行化历史写入；同UTC日连续周期成功累计occurrenceCount/latestAt，不改变createdAt/expiresAt。
- 失败、身份变化、首次和人工认证单独保存；跨日新段，迟到观测另段。90天是约三个月，不是日历月份减法。
- `common/database.py`、`common/event_indexes.py` 安装认证、审计、请求和运行事件查询索引；现有业务库分别有8、6、10、5个索引，包含主键与兼容旧索引。
- 只读检查本机Settings认证保留为90天，authentication_records无expiresAt的记录数为0。没有改动业务数据或再次回填。

## 本次验证

- 认证保留、事件索引、保留计划、基准解析14项通过；认证历史及资源健康31项通过，共45项。
- `scripts/verify_authentication_history_concurrency.py` 在真实Mongo临时库通过，headRevision为33，包含成功与OFFLINE边界，临时库由finally删除。
- `scripts/benchmark_db_growth.py --rows 20000 --compare-indexes` 在真实Mongo随机库通过并自动删除临时库。报告为本机output/db-index-recheck-20260911.json，仅保存查询统计，无凭据或日志正文。

| 查询 | 返回 | 扫描键 | 扫描文档 | 阻塞排序 |
| --- | ---: | ---: | ---: | --- |
| 认证首屏 | 20 | 20 | 20 | 无 |
| 认证深分页 | 20 | 5020 | 20 | 无 |
| 审计筛选 | 50 | 50 | 50 | 无 |
| 请求筛选 | 50 | 50 | 50 | 无 |
| 运行事件筛选 | 50 | 2500 | 2500 | 有 |

候选索引比较没有进一步降低上述成本，因此不重复添加等价索引。运行事件仍计算派生时间，认证前端仍用page/skip；这些限制需要查询结构改造，不能以IXSCAN作为全部优化完成的证据。样本不代表生产长期容量或500路全天验收。

## 工作区边界

独立审核发现旧expiresAt为null时原回填遗漏，本次增加缺失/null共同匹配和更新条件，保留已写入的有效到期时间。新增回归先复现失败，再验证预览、固定90天回填与重复执行0更新；没有直接改写业务库。

最终上述定向测试合计46项通过，Ruff通过。另在真实Mongo随机库验证缺失/null均被预览并回填、已有到期时间保持、重复执行0更新；finally已删除临时库。

前端子代理的导航抽取修改保留在工作区，尚有退出再登录导航门槛待修复；本次未将其纳入数据库完成范围。未连接设备、未生成采集日志、未修改生产端口或部署配置。
