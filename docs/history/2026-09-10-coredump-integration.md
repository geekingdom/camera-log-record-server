# NFS、Coredump 与资源认证集成验证

本记录保存本机阶段验证，不替代当前状态总表。未连接真实设备、未安装本机 NFS、未修改目标 Linux 服务器。

## 下载链路

`scripts/verify_coredump_streaming.py` 使用随机 Mongo 数据库、临时目录和两台隔离环回 HTTP 服务，经过正式 API 代理及节点文件路由。

- 单文件 2,147,483,648 字节；两个客户端同时完整下载，分别约 81.99 和 82.01 秒，字节数与 SHA-256 均一致。
- 8 个并发尾部 Range 请求通过；If-Range 匹配、无效 Range、仅依赖路径 Cookie 的浏览器下载通过。
- 该次验证进程峰值 RSS 113,311,744 字节（macOS ru_maxrss），约 108 MiB；API、节点与客户端在同一进程，因此不是独立服务 RSS 指标。
- 后续 4 MiB 定向复验确认 API 透传 416 的 `Content-Range`。
- 随机数据库、2 GiB 稀疏文件和临时服务日志均已清理。

这证明对应本机 HTTP 场景以有界内存传输，不证明实际 NFS 多设备写入、跨节点链路或生产多人长期容量。

## 文件导出

`scripts/verify_coredump_export_workflow.py` 经主代理独立运行：两台模拟资源的本地目录扫描、固定副本、单文件摘要、多文件 ZIP 成员与正文、取消配额释放和目录记录消失后的清理均通过。使用真实 Mongo 副本集的随机库，所有模拟文件与库在 finally 清理。

## 资源控制

`scripts/verify_resource_health_transactions.py` 经主代理独立运行：已授权的系统停止恢复与用户 STOP 并发，最终任务 STOPPED、恢复标记被清除。该脚本只覆盖这一竞争，不代表全生命周期的所有并发交错。

`scripts/verify_coredump_transactions.py` 经主代理独立运行：Mongo validator 拒绝快照 claim 写入时，总额度预留同步回滚；快照重复释放、导出并发预留与重复释放、取消先提交时没有成功审计均通过。随机验证库和临时目录已删除。

## 自动化与界面

- 主代理后端全量：848 passed，64.63 秒。后续有修改时以总表更新后的最终回归为准。
- 主代理前端类型检查、73 项测试和生产构建通过。
- NFS/Docker/原生部署与平台设置定向 78 项通过；部署器固定星号导出、旧网段键忽略、仅管理项目专属 exports。
- 资源编辑首次回填在 1440/390 视口通过；coredump 文件页通过筛选、批量导出、取消、下载票据及旧请求隔离的浏览器模拟检查。

## 未完成的实机验收

目标 Ubuntu/Debian 的 nfs-kernel-server 安装与开机自启动、设备固件挂载输出兼容、真实 NFS 并行大文件接收、跨节点物理隔离、HTTPS 证书/代理、500 路全天容量和文件可读 P99 均需单独验收。

## 提交后集成修复

基线 `c7c5957` 的 CI `34438823577` 已完成：component-smoke、commit-messages、native-smoke 成功，另外三项失败。实际日志确认：

- backend 的两个新 runtime 测试未显式设置加密密钥，CI 空配置触发 Fernet 校验失败；修复为 `_env_file=None` 和测试专用随机密钥。
- frontend 刷新页面后立即读取管理员导航，异步会话尚未恢复时得到空列表；等待管理员专属导航渲染，仍严格断言 API 文档位于后台配置之后。
- container-smoke 前序验收遗留串口资源，后续压测复用相同 Docker 网关 IP 触发地址唯一约束409；验收 finally 现在按本次资源ID软删并等待异步收尾，不删除真实设备或其日志。

主审另发现取消导出不能立即等同于文件线程停止。新增独立执行状态/租约，取消期间继续刷新执行和业务租约，待复制/ZIP/目录收尾完成后停止续租。维护不会删除有效WRITING作业；失属执行器不能清理其它owner的目录或配额。

本次最终本地回归856项（64.09秒）、Ruff全量、前端类型检查通过；子代理前端73项及生产构建通过，API文档浏览器覆盖1440/390/320（78个接口）。真实Mongo导出脚本重新通过单文件/ZIP/取消释放/TTL清理；事件脚本补充并通过核心转储失败和取消的真实聚合筛选。自建随机库和临时文件已确认清理。

本次修复仍须新提交远端CI复验。没有安装本机NFS或重启业务API/Worker。快照到期与跨节点读者保护、快照发布/释放中断后的孤立文件、真实NFS源文件保留策略仍需完善或验收，不宣称所有崩溃窗口已解决。

## 快照读者与可恢复清理

`caa74fd`远端CI `34439766876`六作业全部成功，包含完整容器部署、8路合成协议采集及原生部署；隔离容器/测试卷清理成功。该结果确认上一节三项修复，不能替代后续变更验证。

后续实现`SnapshotReader`与独立生命周期模块：读者申请及退休使用同一catalog文档CAS，退休拒绝新消费者，但可凭仍有效的父读者令牌委托内部跨节点子读者。子读者首次获得授权后独立续租；HTTP和本地复制每块读取前检查有效性。清理按RETIRING、DELETING、RECEIVING推进，unlink和配额归还完成前始终保留snapshot路径及令牌。

- 主代理后端全量868项（65.40秒），之后补充路由用例的消费端5项通过；子代理相关39项通过。
- 真实Mongo脚本`verify_coredump_readers.py`验证12个并发读者（含其他协调节点身份）、12轮申请/清理竞争、退休后拒绝普通新读者、有效委托以及父关闭后子独立续租。确认NFS源保留、配额归零、随机库及临时文件清理。
- 单测注入unlink失败、unlink后配额释放失败，确认DELETING记录保留且下一轮恢复成功；旧清理令牌不能删除新冻结副本。
- 节点真实ASGI路由验证RETIRING无header返回409、错误父令牌404、有效父令牌Range返回206且摘要一致；响应结束只保留父读者，全部关闭后清理快照并保留源。
- 导出工作流真实Mongo复跑通过；前端73项、类型检查及构建通过，coredump桌面/390浏览器验收通过且主代理查看手机截图。

容器CI加入上述读者脚本及导出工作流脚本。此阶段没有实机NFS传输，没有重新执行2GiB压力实验；历史无catalog孤立PUBLISHED声明及断电/网络失联矩阵仍未覆盖。

## 孤立副本回收与连接复核

`872c02b`的CI `34441011617`六作业成功，包括上一节新增的真实Mongo读者/导出脚本。本次在该基线上处理历史孤立声明：过期RESERVED/PUBLISHED且无catalog引用，或此前失败的RECLAIMING，按本节点的fileId/version/token精确删除私有副本后归还配额。源文件、替代token、未登记文件不删除；标识损坏、符号链接或物理错误保留声明并记录日志。

`scripts/verify_coredump_orphans.py`首先在旧基线上真实Mongo复现副本未清理的失败，finally完成随机库和目录清理。实现后主代理复跑通过旧已发布副本、未发布partial/core回收，新token及NFS源保留、重复回收配额不重复扣减；1000条RELEASED历史记录下到期候选查询`totalDocsExamined=2`。同脚本已加入容器CI。

主代理后端全量878项（66.62秒）及Ruff通过；孤立回收专属9项覆盖精确归属、失败后重跑、错误单项不阻塞其他token、四种catalog引用保护、symlink拒绝、取消时等待删除线程、非法标识/版本和不同节点/未到期声明保护。真实Mongo读者12轮竞争复验通过。所有实验均使用临时目录和随机库并清理。

子代理独立SSH/Telnet生命周期审查未发现新确定性缺口；connections/runtime/pause/scheduler/worker/isolation/debug定向66项通过。默认10秒无打印重连、关闭/取消abort、暂停禁重连及恢复预算保留有源码与测试证据；Telnet保活失败到重连、首次关闭失败后人工隔离恢复的端到端覆盖仍可补充。没有操作真实设备或重启业务服务，不能据此证明设备端五个SSH槽位已经在实机释放。

## 独立 Linux NFS 验证入口

`72ae569`的CI `34442294566`六作业全部成功，确认孤立副本回收及此前部署回归。本轮新增`scripts/verify_nfs_service.py`和独立`nfs-smoke`作业：在临时Ubuntu主机调用正式NFS配置器，以NFSv3/v4双挂载点并发写入各64MiB，校验客户端/服务器摘要和10001:10001映射；重复配置后重新挂载确认内容保留。

主审要求补齐挂载命令失败但内核已挂载的收尾保护、导出撤销失败保留目录、实际清理结果与保留路径报告、配置命令超时和输出捕获。定向22项（含生产NFS部署规则10项）及Ruff全量通过；暂未执行Linux实际挂载，等待提交后的独立CI。验证仅操作随机临时路径，不在开发Mac上安装NFS，不修改真实设备日志。

`66c3dec`的CI `34443227583`中NFS作业已通过。主代理下载JSONL确认v3/v4两路各67108864字节、UID/GID均10001:10001，摘要分别为`3402745c01bb748d61251da1ce17e683c8f816018c1dc0dbc7b649c6fc186412`及`a6997462ce5008e304ff464aeedd731bf8b07f5cbd925f3f0728b256811ba24c`；重复配置重挂成功，导出撤销、刷新及根删除均true，无保留路径或错误。此前主代理全量890项通过（66.52秒）。

后续独立契约审查指出首轮挂的是父根，真实Worker直接挂设备IP子目录。验证已改为服务端预建并赋权IP目录、直接挂载该子目录，再校验双路传输和重挂；补充2项契约测试，与监控/部署合计31项及Ruff通过。此补充等待新提交的Ubuntu实机结果，父根成功不能自动证明子目录成功。

最终代码`204f9ee`在CI `34443638369`的NFS作业成功，主代理与独立审核均收取JSON：`mountTarget`分别为本次临时导出下`192.0.2.101`、`192.0.2.102`，协议v3/v4，两路各67108864字节，摘要与首轮相同且属主10001:10001。重配重挂成功；`exportRemoved/exportsRefreshed/rootRemoved=true`、`preservedPaths/errors=[]`。前一CI `34443227583`七作业均通过；实机范围为一台Ubuntu的双挂载点，不代表物理海康设备及跨主机容量通过。本地只保留小型JSON证据，没有新增采集日志或coredump大文件。

## 共享 Coredump 负责人

用户确认已有正常采集SSH负责人时，其他新建SSH表单共享显示开启、禁用重复开关并注明任务；共享展示不写成新任务`enableCoredumpMonitor=true`。状态接口只承认健康资源、有效资源租约、完整任务/run/generation/node身份、COLLECTING与RUNNING及新鲜节点心跳。旧运行挂载结果不能复用于当前运行，运行重连先清空旧状态。

后端独立审核通过，主代理全量904项（67.35秒）、Ruff全量通过。真实Mongo脚本`verify_coredump_monitor_ownership.py`独立复跑验证同资源一True一None、不同资源独立、停止/失属拒绝和过期/释放接管，确认无设备访问，随机库及临时目录清理；脚本已接入容器CI。

前端独立轮询按资源代次和单飞处理迟到请求；提交前刷新，查询失败中止保存，先勾选后切共享资源仍提交false。主代理78项及生产构建通过；专属模拟浏览器覆盖共享状态、本人可编辑、协议/资源切换、负责人停止、查询失败及1440/390布局。主代理发现手机截图未滚到控件、桌面说明粘连，已补滚动等待及组件纵向排版。隔离真实API/Worker的`browser_smoke.mjs`通过WS与小时下载，unexpectedConsoleErrors=0，随机数据库和日志目录均已清理。现有业务API/Worker未重启。

## API 分类栏滚动遮字

分类栏处于固定高度目录的纵向Flex容器中，默认收缩会压缩按钮与滚动条间隔。仅在`.api-groups`设置`flex:0 0 auto`及`scrollbar-gutter:stable`，保留横向滚动和既有12px底部留白。

独立1440px浏览器临时覆写旧/新样式对照：横向内容1349px、容器287px；旧版高度约16px、按钮距容器底约6.28px，12px留白断言失败；新版高度39px、按钮距容器底13px且距client底12px，断言通过。当前Mac使用overlay滚动条，不能以此声称已验证Linux内嵌条尺寸。浏览器脚本补首尾分类完整进入滚动视口和底部留白断言，1440/390/320专属验收通过。

主代理完整页面回归首次发现旧资源快捷建任务夹具对新`coredump-monitor`路径返回空内容，保存前校验失败；已补明确inactive接口夹具，不放宽产品错误处理。后续完整回归继续执行，临时预览均由脚本关闭。
