# 运行事件时间与索引验证

起点455a70b。CodeGraph同步和runtime_event_page影响检查后核对全部正式events写入路径，均已有createdAt；历史detectedAt兼容导致默认列表每次派生排序。

## 实现

- 新增默认createdAt/_id倒序索引，不删除既有筛选索引。
- 每次查询探测旧时间，无旧格式时用持久createdAt排序；旧库保留原兼容路径。createdAt显式null的旧记录原来被时间范围漏查，新增回归先复现后修复。
- 结果和级别派生筛选前置持久时间排序，过滤后才分页；不修改派生结果语义。
- 新增低峰回填工具，默认预览，批次与总量受限；CAS防覆盖已有值或并发检测时间变化。只复制标量BSON日期，日期数组和无时间记录不造时间。

## 验证

- 后端全量1239项通过；后续日期数组与并发检测时间补强后，回填/事件/索引专项23项通过。Ruff和diff通过，独立审核无阻塞问题。前端未修改，不重复前轮115项和浏览器验证。
- 真实Mongo事件结果/级别优先级及分页检查通过，临时库已删除。
- 新验证器两万条样本：默认、taskId、nodeId、type查询各返回50条，扫描50键/50文档，无SORT；派生outcome扫描96候选、实际页面50条，无SORT；无旧记录的探测0键/0文档。
- 缺失/null创建时间迁移前后列表与时间范围一致；已有创建时间优先，日期数组不回填。随机库finally删除，不创建采集日志。
- 本机预览2条待回填，apply更新2条，剩余和不可迁移均0。API重载后health为ok，events实有6个索引，包括events_created_at_id；旧格式0。
- 前一提交455a70b的CI34571226122成功，不当作本轮未提交修改的远端验证。
- 新真实Mongo验证器已加入container-smoke，工作流解析通过；新增步骤的远端执行需在提交后确认。

## 边界

探测和分页不是同一快照，部署应先升级所有旧写入端。尚未迁移或含无效非空检测时间的旧库继续兼容排序。结果推导、总数及深skip仍有成本；本次无阻塞排序只证明代表性实际Explain，不代表生产长期成本或500路全天负载。
