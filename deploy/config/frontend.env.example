# 单独前端：./deploy-frontend.sh --init，然后编辑 .env.frontend。
# 必填：前端容器能访问的后端根地址，不含路径、用户名和密码。
# 后端同服务器时使用服务器内网IP，不要填127.0.0.1（它指向前端容器本身）。
BACKEND_UPSTREAM=http://192.0.2.20:18080
# 可修改：Web访问端口；支持WebSocket实时日志和流式下载。
FRONTEND_PORT=5175
# 本组件只部署静态前端与Nginx代理，不需要数据库密码或设备密钥。
# 后端 FORWARDED_ALLOW_IPS 必须信任本前端实际连接来源；平台白名单仍校验用户来源IP。
