# 工程约定

## 工作方式

- 修改前使用 `codegraph sync .` 更新索引，再使用 `codegraph impact <符号>` 检查影响范围。图谱用于辅助定位，最终必须通过源码和测试验证结论。
- Python 公共模块放在 `backend/camera_logs/common`，任务、命令、采集、日志、节点及管理功能分别归入对应子目录。前端共享模块位于 `shared`，功能界面位于 `features`，装配入口位于 `app`。
- 所有源码使用中文模块说明，并用中文解释公共函数职责、状态转换、并发控制和异常处理依据。避免以压缩成一行的代码规避模块大小要求；单文件建议不超过 500 行。

## 不变量

- 每设备采集连接必须在停止、暂停、失败及取消后主动回收。手动和定时命令复用采集连接，不额外建立 SSH 连接。
- 同一会话只有一个接收器与命令发送队列，写入按接收顺序进行。日志重复正文保留；结果不明的写入不可整批重试。
- 行首添加服务器上海时区时间前缀，跨分包同一行只添加一次。压缩命名包含安全任务名、IP、小时范围及唯一片段信息。
- SSH 暂停保留运行和命令预算，禁用自动重连；停止后启动则产生新运行。网络断开和持续十秒无输出允许重新连接并初始化。
- 模板复制配置而不共享任务计数。密码不回传，不写入服务日志或源码；下载及设备正文不得入库。

## 验证和提交

```sh
pytest -q
ruff check backend tests scripts
cd frontend
npm test
npm run build
```

影响布局、实时显示或下载时运行 `scripts/browser_smoke.mjs` 并检查桌面及移动端截图。与性能相关的修改应执行合成校验，并清晰区分短时本机结果和 24 小时生产容量验收。

仓库固定为 `git@github.com:geekingdom/camera-log-record-server.git`。执行 `scripts/install-git-hooks.sh` 安装钩子，每次提交使用中文标题及详细中文正文，正文包含 `背景：`、`变更：`、`测试：`、`影响：`。CI 同步校验此规则。
